export {
  generateLayerId,
  generateSourceId,
  generateControlId,
  resetCounters,
} from './id-generator';

export { MapExtendError, validateUrl, validateBasemapName, clamp } from './validation';
